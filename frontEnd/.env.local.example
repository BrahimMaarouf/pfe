# Backend inference API (FastAPI). No trailing slash.
# After: cd backEnd && python -m venv .venv && .venv\Scripts\activate && pip install -r requirements.txt && python main.py
NEXT_PUBLIC_API_URL=http://localhost:8000
